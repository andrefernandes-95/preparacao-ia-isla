{
  "name": "preparacao-ia", "version": "1.0.0", "private": true,
  "description": "Percurso de preparação para a pós-graduação em IA — HTML estático por aula",
  "type": "module",
  "scripts": {"build": "node scripts/build.mjs", "test": "node scripts/check.mjs && node scripts/check-bases.mjs", "start": "node scripts/serve.mjs"},
  "engines": {"node": ">=20"}
}
